/** 共享常量 */

export const DEFAULT_MODEL_PROVIDER = 'openai-compatible'
export const DEFAULT_MODEL_ID = 'deepseek-chat'

// SSE 重连间隔（ms）
export const SSE_RECONNECT_INTERVAL = 3000

// 心跳间隔（ms）
export const HEARTBEAT_INTERVAL = 30000

// 消息 ID 长度
export const ID_LENGTH = 21
